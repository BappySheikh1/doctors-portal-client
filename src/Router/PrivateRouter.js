import React, { useContext } from 'react';

const PrivateRouter = ({children}) => {
    const {}=useContext()
    
    return (
        <div>
            
        </div>
    );
};

export default PrivateRouter;